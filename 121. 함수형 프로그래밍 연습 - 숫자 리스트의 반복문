
전 학습에선, 우리는 문자열의 리스트를 만들고 함수형 프로그래밍으로 리스트를 출력했다

이제 연습 문제 하나에 대해 이야기해 보도록 하자 
우리가 하고 싶은 것은 숫자의 리스트를 만든 다음 그것을 루프로 돌린 후,
함수형 프로그래밍으로 출력하는 것이다
연습으로 JShell 안에서 시도해 보도록 하자

먼저 List를 만들자
jshell> List<Integer> list = List.of(1,4,7,9);
list ==> [1, 4, 7, 9]

루프해서 함수형 프로그래밍으로 이 안의 모든 값들을 출력하고 싶다..
무엇을 하고 싶나? 우리가 하는 것이 무엇인지, 이 안의 각 요소에 대해 무엇을 하고 싶을까?
-> 는 람다 표현의 문법의 일부이다
그래서, 우리는 여기에 존재하는 모든 요소를 출력하고 싶은것이다, 맞지?
jshell> list.stream().forEach(
   ...>        element -> System.out.println(element)
   ...>        );
1
4
7
9
보다시피 이것은 매우 단순한 코드이다, 루프 없이 실행 가능한 것이다, 
이것이 작동하는 구조는 각 요소가 흐름되고 그리고 각 요소들에 대해 이 코드가 실행되는 것이다.


//다음 단계에서는 함수형 프로그래밍의 조금더 복잡한 것들로 넘어가도록 하겠다.
